name: babibeauty_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dio: ^5.7.0
  go_router: ^14.2.0
  shared_preferences: ^2.3.2

  firebase_core: ^2.30.0
  firebase_auth: ^4.19.0
  firebase_messaging: ^14.7.10

  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.2   # 🔥 مهم للـ scheduling

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true